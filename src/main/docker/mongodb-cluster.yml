version: '2'
services:
    katalog-mongodb:
        image: mongo:4.0.2
        ports:
            - "27017:27017"
        command: mongos --configdb katalog-mongodb-config
    katalog-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    katalog-mongodb-config:
        image: mongo:4.0.2
        container_name: katalog-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
